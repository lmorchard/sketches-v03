{
  "name": "sketches-v03",
  "version": "1.0.0",
  "description": "For years, I've tinkered with game development on the web. But, I haven't finished (m)any games. So, I decided to just focus more on finishing little interesting sketches of graphics and sound.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && npm-run-all -p watch serve",
    "build": "node build.js",
    "serve": "reload -v -w ./dist -f index.html -d ./dist -p 9980",
    "watch": "onchange 'src/**/*' -- npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.25.2",
    "glob": "^11.0.2",
    "mkdirp": "^3.0.1",
    "npm-run-all": "^4.1.5",
    "onchange": "^7.1.0",
    "recursive-copy": "^2.0.14",
    "reload": "^3.3.0",
    "rimraf": "^6.0.1"
  },
  "dependencies": {
    "pixi-filters": "^6.1.2",
    "pixi.js": "^8.9.1",
    "seedrandom": "^3.0.5"
  }
}
